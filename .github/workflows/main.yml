name: Docker Login and Push

on:
  push:
    branches:
      - main  

jobs:
  build_and_push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Docker Login
        uses: docker/login-action@v1
        with:
          username: atest
          password: ${{ secrets.HARPOON_TOKEN }}
          registry: host.docker.internal:5050

      - name: Build and Push Docker Image
        run: |
          docker build -t host.docker.internal:5050/atest/harpoondemofront:latest .
          docker push host.docker.internal:5050/atest/harpoondemofront:latest